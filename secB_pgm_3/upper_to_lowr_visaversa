#include <stdio.h>
#include <ctype.h>
#include <string.h>

int main() 
{
    char str[100];

    printf("Enter a string: ");
    fgets(str, sizeof(str), stdin);

    for (int i = 0; str[i] != '\0'; i++) 
    
   /* Why do we use it in loops?

When we write:

for (int i = 0; str[i] != '\0'; i++)


We’re telling the loop:

“Keep going through each character of the string until you reach the end (the null terminator).”

Because after the last visible character in the string, the system automatically places a '\0' to mark the end.*/
    
    {
        if (islower(str[i]))
        {
            str[i] = toupper(str[i]);  // convert lowercase → uppercase
        }
        else if (isupper(str[i])) 
        {
            str[i] = tolower(str[i]);  // convert uppercase → lowercase
        }
        // else leave it unchanged
    }

    printf("Toggled case string: %s\n", str);
}

